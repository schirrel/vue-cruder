import { FormOptions } from "@/builders/models";

export const createProvider = (
  resource: string,
  formOptions: FormOptions
) => {
  // create service
  // create form
  // create page
  // create route
};
